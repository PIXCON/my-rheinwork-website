# Production-only overrides (used by `hugo` / `hugo --minify`, e.g. the GitHub Pages deploy).
# `hugo server` runs in the development environment and ignores this file.
baseURL = "https://rheinwork.com/"
