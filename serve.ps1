# Lokaler Dev-Server unter http://localhost:1313/
# Die Umgebung "development" nutzt automatisch baseURL "/" (siehe config/_default/hugo.toml).
# Der GitHub-Pages-Pfad gilt nur im Production-Build (config/production/hugo.toml).
hugo server --disableFastRender
